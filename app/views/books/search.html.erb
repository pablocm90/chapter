<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= search_title = (params[:query].present?) ? "Books matching #{params[:query]}" : "All our books" %></h1>
  </div>
</div>

<%= render 'shared/cards' %>

<!-- HTML Markup -->
